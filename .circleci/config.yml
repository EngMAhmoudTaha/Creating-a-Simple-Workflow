version: 2.1

commands:
  print_pipeline_id:
    steps:
      - run: echo ${CIRCLE_WORKFLOW_ID}

jobs:
  my_job:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run: 
          command: return 1
      - run:
          command: echo "hello errors"
          when: on_fail


workflows:
  my_workflow:
    jobs:
      - my_job